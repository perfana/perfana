# Phase 5a Audit Migration — Decisions & Burndown

**Started:** 2026-05-02. **Spec:** [`docs/superpowers/specs/2026-05-02-rbac-phase5a-audit-completion-design.md`](../specs/2026-05-02-rbac-phase5a-audit-completion-design.md). **Plan:** [`docs/superpowers/plans/2026-05-02-rbac-phase5a-audit-completion.md`](../plans/2026-05-02-rbac-phase5a-audit-completion.md).

This doc is the self-contained reference for the Phase 5a audit-logging migration: the decisions, the migration burndown, and the priority order. The ESLint rule `audit-mutation-must-log` reads `apps/api/.audit-migration-allowlist.json` — every entry there is a service file that mutates an `OwnedResource` without yet calling `auditService.log{Create,Update,Delete}`. As services migrate, they leave the allowlist; when the allowlist empties and the drift agent reports zero new sites for two consecutive runs, Phase 5a's audit migration is done.

## Decisions

Reproduced from the spec for self-contained reference.

| # | Decision | Rationale |
|---|---|---|
| Q1 | Split Phase 5 into 5a (audit) and 5b (RLS); ship 5a first | Independent subsystems; smaller spec lands faster, RLS still needs a benchmarking spike |
| Q2 | Design for forensics + activity feed + (default-everything foundation) | User chose D ("all three") — driver is foundational; security monitoring slice deferred to follow-on phase |
| Q3 | Skip ACCESS events entirely | Reads dominate volume 100:1; reconstructible from logs/metrics; activity feed is a feed of *changes* |
| Q4 | Service-layer explicit `auditService.log{Create,Update,Delete}` calls — NOT a TypeORM subscriber, NOT a method decorator | Reliable diffs, accurate user context, allows field filtering. Manual ⇒ ESLint-enforced (Phase 3 muscle: lint rule + allowlist + burndown + drift agent) |
| Q5 | Native PostgreSQL declarative partitioning, partition by month, single ~24-month retention window | Greenfield (no production audit data yet); `DROP PARTITION` is the right primitive; no extra Postgres extension |
| Q6 | Skip auth events (LOGIN/LOGOUT) entirely | Keycloak is source of truth for human auth; API-key auth events out of scope for 5a |
| Q7 | Two read endpoints: admin filterable + per-resource history | Frontend deferred but backend complete; future activity tab + admin tool both buildable on top |
| Q8 | Skip ACCESS_DENIED in 5a | Phase 3's `withOrgFilter` already silently drops unauthorized rows; explicit deny instrumentation is a security-monitoring concern, deferred |
| Q9 | Repurpose `AuditInterceptor` as `nestjs-cls` request-context provider; no longer writes audit rows | Service-layer audit calls read envelope (userId, IP, UA, requestId) from CLS store. Avoids 7-arg method calls. ALS infrastructure also amortizes into 5b's RLS GUC plumbing |
| Q10 | Per-entity static `auditableFields: string[]` allowlist; default = nothing logged | Default-safe against credential leaks. Adding a sensitive column to an entity does not silently leak it into the audit log |
| Q11 | Per-resource history endpoint shares the resource's existing RBAC | "If you can see it, you can see who edited it." No new capability invented |

## ESLint rule + infrastructure exemption

The rule `audit-mutation-must-log` (`apps/api/eslint-rules/audit-mutation-must-log.js`) flags any service `MethodDefinition` that calls a mutation method (`save`, `delete`, `remove`, `update`, `insert`) on a receiver matching `/repo|Repository|manager/i` without a paired `auditService.log{Create,Update,Delete}` call in the same method body.

**Permanent infrastructure exemption** (hardcoded in the rule, never on the burndown):

- `apps/api/src/modules/audit/audit.service.ts` — the audit writer itself.
- `apps/api/src/modules/audit/audit.module.ts` — wiring.
- `apps/api/src/common/services/authorized-base.service.ts` — base class for authz-aware services; persists internal flags without audit semantics.
- `apps/api/src/common/repositories/typeorm-base.repository.ts` — generic repository wrapper; pure infrastructure.

**Allowlist** (`apps/api/.audit-migration-allowlist.json`): files that mutate but don't yet call `auditService.log*`. Migration removes the entry; the burndown table tracks progress.

## Burndown

Unit: files in `apps/api/.audit-migration-allowlist.json`.

| Date | Total at start | Migrated this round | Remaining | Notes |
|---|---|---|---|---|
| 2026-05-02 | 56 | 0 | 56 | Initial. PR4 lands rule + allowlist + drift + snapshot. |
| 2026-05-03 | 56 | 1 | 55 | PR5: `api-keys.service.ts`. `ApiKey.auditableFields = ['description', 'roles', 'validUntil', 'organization_id']` (excludes the bcrypt `apiKey` hash and the per-auth `lastUsed` touch). `logCreate` after persist + cache; `logDelete` before cache invalidation and `repo.delete`. `ApiKey` doesn't formally `implements OwnedResource` because `created_by?` is nullable on legacy keys — call sites cast `as unknown as OwnedResource`; `AuditService.dispatch` only reads `id` and `organization_id` so the cast is sound. Repository file (`apps/api/src/repositories/api-key.repository.ts`) stays on the allowlist — repository-layer audit migration is a separate workstream. |
| 2026-05-03 | 55 | 2 | 53 | PR6: `organizations.service.ts` + `organization-members.service.ts`. `Organization.auditableFields = ['name', 'description']` and `OrganizationMember.auditableFields = ['user_id', 'roles', 'organization_id']`. Organization is the root of the access-control hierarchy and has no `organization_id` column itself, so every Organization audit envelope is set via `organizationIdOverride: <org.id>` (CREATE after persist, UPDATE with cloned before-snapshot, DELETE before the cascade transaction). `OrganizationMember` rows carry `organization_id` natively, so the dispatch picks it up with no override. Org-level cascade deletions (teams, SUTs, test_runs, organization_members) intentionally not individually audited — they are implied by the org-DELETE row, and the raw `manager.query('DELETE …')` calls would not surface to the audit lint rule's `repo|Repository|manager.<MUTATION_METHODS>` matcher anyway. Module registers both `organizations` and `organization-members` resource types with `AuditResourceRegistry`. Snapshot picked up `OrganizationMember` (organizations entity is excluded from the snapshot because the snapshot filter is "entities with an `organization_id` column"); `Organization.auditableFields` is therefore not pinned by the snapshot — acceptable trade-off, deviation from the snapshot scope is out of PR6 scope. |
| 2026-05-03 | 53 | 2 | 51 | PR7: `teams.service.ts` + `team-members.service.ts` (parallel to PR6). `Team.auditableFields = ['name', 'description', 'organization_id']` and `TeamMember.auditableFields = ['user_id', 'roles', 'team_id']`. The org-context handling is inverted relative to PR6: Team rows carry `organization_id` natively so the dispatch picks it up without override, while TeamMember rows carry only `team_id` and need `organizationIdOverride: member.team.organization_id` (resolved via the eagerly-loaded `team` relation) so org-admin scoped queries see membership events. CREATE after persist, UPDATE with cloned roles array in the before-snapshot, DELETE before `repo.remove`. Team's `restrict_to_team_members` flag is intentionally excluded from `auditableFields` — it's a visibility hint, not a security boundary, and would add diff noise. Module registers both `teams` and `team-members` resource types with `AuditResourceRegistry`. Snapshot picked up `Team` (because Team has an `organization_id` column); `TeamMember.auditableFields` is therefore not pinned by the snapshot — acceptable trade-off, mirrors the PR6 trade-off in reverse. 16 new spec assertions across the two services cover the create/update/delete log invariants, including org-override resolution from the team relation and "log before mutation" ordering. |
| 2026-05-03 | 51 | 3 | 48 | PR8 (bucket 1): `create-test-run.handler.ts`, `update-test-run.handler.ts`, `update-adapt-config.handler.ts` removed from allowlist. Five additional handlers wired (not on the allowlist because they bypass the lint matcher via raw `dataSource.query`): `update-tags`, `update-annotations`, `update-analysis-start-offset`, `delete-test-run`, plus the existing `update-test-run` raw-SQL path. `init-test.handler.ts` intentionally not wired — it generates a unique `test_run_id` string and may create a SystemUnderTest via the lookup service, but does not mutate `TestRun`. `TestRun.auditableFields` pinned by the snapshot — 16 user-mutable fields covering identity (`testRunId`), test outcome (`completed`, `abort`, `abortMessage`, `consolidatedResult`), test config (`adaptConfig`, `analysisStartOffset`, `duration`, `plannedDuration`, `variables`, `expires`, `expired`), CI metadata (`applicationRelease`, `ciBuildResultsUrl`), and editorial annotations (`annotations`, `tags`). Excluded: identity / immutable axes (`id`, `systemUnderTestId`, `testEnvironment`, `workload`), timestamps (`startTime`, `endTime`, `createdAt`, `updatedAt` — bumped on every save), ownership tracking (`organizationId`, `teamId`, `createdBy`, `updatedBy`), and system-derived fields (`status`, `isStale`, `staleDetectedAt`, `valid`, `reasonsNotValid`, `dataWarnings`, `deepLinks`, `deletionStatus`). Critical wrinkle: TestRun's `organization_id` column maps to camelCase property `organizationId`, so `AuditService.dispatch` cannot read it from `ref` directly — every TestRun call site passes `organizationIdOverride: testRun.organizationId`. UPDATE handlers that previously skipped the pre-mutation fetch (or used a narrow `select`) now load the full row first to seed the diff. DELETE handler uses `logDelete` *before* the cascade transaction (mirrors PR6 org-delete pattern); cascaded child-table deletions (`ds_*`, `check_results`, `transactions`, etc.) intentionally not individually audited. 15 new spec assertions across all 7 handlers in `__tests__/handlers-audit.spec.ts`. Module registers `'test-runs' → TestRun` with `AuditResourceRegistry`. Buckets 2 and 3 deferred — see "Notes from PR 8" below. |
| 2026-05-03 | 48 | 1 | 47 | PR9: `dynatrace.service.ts`. Three new `auditableFields` declarations: `DynatraceConfig` (`host`, `label`, `dynatraceType`, `perfanaTestRunIdAttribute`, `perfanaRequestNameAttribute` — `apiToken` and `platformApiToken` are encrypted credentials and excluded by name); `DynatraceQuery` (14 query-definition fields covering parent linkage, scope keys, panel identity, the DQL itself, and metric naming/template-variable metadata — `metricsSourceId` excluded because it's derived from a repository-side upsert, not user input); `DynatraceEntityMapping` (8 mapping-definition fields). Service emits `auditService.log{Create,Update,Delete}` for all 10 user-facing mutations: config CRUD (`create`/`update`/`delete`), query CUD (`createQuery`/`createQuerySmart`/`bulkImportQuery`/`updateQuery`/`deleteQuery`), and entity-mapping CD (`createEntityMapping`/`deleteEntityMapping`). All three entities exhibit the same camelCase property / snake_case column mismatch as TestRun (`@Column({ name: 'organization_id' }) organizationId!`), so every Dynatrace audit call passes `organizationIdOverride: row.organizationId`. The query/mapping repository helpers return mapped DTO objects (via `mapEntityToDtoFields` / `mapEntityMappingToDtoFieldsWithLabel`) rather than entity instances — so the service wraps each DTO with `Object.assign(new DynatraceQuery(), dto)` (and the `DynatraceEntityMapping` analog) before handing it to `AuditService`. This restores the prototype so `AuditService.dispatch`'s `ref.constructor.auditableFields` lookup resolves to the declared array. The two helpers `toQueryAuditRef` / `toMappingAuditRef` localize the wrapping. `bulkImportQuery` shared-UUID mode emits one `logCreate` per persisted row (per the audit architecture's "one row per entity" rule); the non-shared mode delegates to `createQuerySmart` per row, which already emits its own audit row. `createHostMetricQueries` is intentionally not directly audited — it calls `createQuery` per metric (already covered) and additionally invokes `repository.ensureArtificialDashboardExists` and `repository.createDsCompareConfigForMetric`, both of which mutate via raw `manager.query('INSERT …')` against `grafana_dashboards` / `application_dashboards` / `ds_compare_config` (system-derived bootstrap rows, bucket-2 pattern). Module registers three resource types — `dynatrace-configs`, `dynatrace-queries`, `dynatrace-entity-mappings` — with `AuditResourceRegistry`. Snapshot picks up all three entities (each owns an `organization_id` column) and records the new `auditableFields` arrays. `dynatrace.repository.ts` stays on the allowlist as a separate workstream (mirrors the api-keys precedent: repository-layer audit migration is its own pass). Spec adds 11 audit assertions across the three entities — CREATE/UPDATE/DELETE invariants, before/after diff carry-through, `logDelete` ordering before `repository.delete` (invocationCallOrder check), and bulk-import per-row fan-out. |
| 2026-05-03 | 44 | 3 | 41 | PR11: `pyroscope-instances.service.ts` + `tracing-instances.service.ts` + `tracing-services.service.ts` (bundled — closes the sensitive-credentials integrations group). Three new `auditableFields` declarations: `PyroscopeInstance` (`label`, `pyroscopeUrl`, `backendUrl`, `pyroscopeStandAlone` — no credential columns on this entity); `TracingInstance` (`label`, `tracingUrl`, `tracingApiUrl`, `tracingUi`, `tracingIframeAllowed` — likewise no credentials); `TracingService` (`systemUnderTestId`, `testEnvironment`, `workload`, `tracingInstanceId`, `serviceNames` — the scoping keys, the FK to `TracingInstance`, and the service-name list). All three entities exhibit the camelCase property / snake_case column mismatch (`@Column({ name: 'organization_id' }) organizationId!`), so every audit call passes `organizationIdOverride: row.organizationId` (matching the GrafanaInstance / Dynatrace / TestRun precedent). `PyroscopeInstancesService` and `TracingInstancesService` follow the now-standard shape: `logCreate` after `repo.save`, `logUpdate` with cloned `before` snapshot via `Object.assign(new Entity(), entity)`, `logDelete` before `repo.remove`. `TracingServicesService.createOrUpdate` is the wrinkle — the upstream `repository.createOrUpdate` performs an internal `findByExactMatch` upsert, so the service does its own pre-check via `findByExactMatch` to split CREATE vs UPDATE for accurate audit semantics. The cost is one extra SELECT on this rarely-called write path; the benefit is "one row per logical user action" auditing instead of always-CREATE-shaped rows that mislabel updates. `TracingServicesService.update` clones `before` before delegating to the base repository's in-place `update`, then re-fetches `after` via `findById`. `TracingServicesService.delete` logs DELETE before `repository.delete`. The three modules each import `AuditModule` and register their resource type — `pyroscope-instances`, `tracing-instances`, `tracing-services` — with `AuditResourceRegistry` in `onModuleInit`. Snapshot picks up all three entities (each owns an `organization_id` column) and records the new `auditableFields` arrays. `tracing-service.repository.ts` stays on the allowlist as a separate workstream (mirrors the api-keys / dynatrace precedents). Three new spec files (one per service) cover the audit invariants: CREATE/UPDATE/DELETE, the createOrUpdate CREATE-vs-UPDATE branch, before/after diff carry-through, and `invocationCallOrder` ordering checks for "log before mutation". |
| 2026-05-03 | 47 | 3 | 44 | PR10: `grafana-instances.service.ts` + `grafana-dashboards.service.ts` + `application-dashboards.service.ts` (bundled — second of the sensitive-credentials integrations group, parallel to PR9). Three new `auditableFields` declarations on the corresponding entities: `GrafanaInstance` (`label`, `client_url`, `server_url`, `orgId`, `username`, `snapshotInstance` — `apiKey` and `password` are encrypted credentials and excluded by name); `GrafanaDashboard` (12 dashboard-identity fields covering grafana linkage, uid/slug/name/uri, templating variables, tags, sut-membership array, and template metadata — `panels`, `variables`, `grafanaJson`, `applicationDashboardVariables`, `templateTestRunVariables`, `templateCreateDate`, and `updated` are bulk Grafana-derived JSON re-synced by the grafana-sync service and excluded as system-derived); `ApplicationDashboard` (14 fields covering SUT/environment scope, grafana linkage, dashboard identity, tags, variables, replaced templating variables, snapshot timeout, and metrics-source link). All three entities use camelCase property / snake_case column naming for `organization_id` (matching the TestRun + Dynatrace precedent), so every audit call passes `organizationIdOverride: row.organizationId`. `GrafanaInstancesService.update` mutates the loaded entity in place before `repo.save`, so the service captures a `before` snapshot via `Object.assign(new GrafanaInstanceEntity(), entity)` to keep the prototype intact for the diff. `GrafanaDashboardsService.update` and `.remove` previously delegated to the service-layer `findOne` for the access check (which mapped entity → DTO and lost the prototype); the migration replaces those calls with a direct `repo.findOne` + `verifyOrgAccess(entity, …)`, holding the same DB round-trip count and the same access semantics while keeping the entity instance for the audit pipeline. `ApplicationDashboardsService.delete` logs DELETE for the `ApplicationDashboard` *before* the cascade transaction (mirrors PR8 / PR9's "log before mutation" pattern); when `deleteFromGrafana=true` and the linked `GrafanaDashboard` is unused by any other SUT (orphaned), it additionally logs DELETE for the sibling `GrafanaDashboard` row. Cascaded `benchmarks` deletions and the `usedBySut` array maintenance update on `GrafanaDashboard` are intentionally not individually audited — same bucket-2 reasoning as test-runs (cascade noise at ingestion-rate volumes, implied by the parent DELETE row). `GrafanaModule` registers all three resource types — `grafana-instances`, `grafana-dashboards`, `application-dashboards` — with `AuditResourceRegistry`. Snapshot picks up all three entities (each owns an `organization_id` column) and records the new `auditableFields` arrays. Spec adds 10 audit assertions across the three services: CREATE invariants on each, UPDATE invariants with cloned-`before` and full-DTO-reload `after`, DELETE invariants with `invocationCallOrder` (or txn-not-yet-started) ordering checks, and a sibling-DELETE assertion on `application-dashboards.delete` when the linked GrafanaDashboard is orphaned. |
| 2026-05-03 | 41 | 3 | 38 | PR12: `graph-presets.service.ts` + `trends-presets.service.ts` + `compare-presets.service.ts` (bundled — closes the user-owned customization presets group). Three new `auditableFields` declarations on the corresponding entities: `GraphPreset` (`name`, `description`, `testRunId`, `userId`, `seriesConfig`, `chartOptions`, `isGlobal` — covers identity / scope, the JSON content of the preset, and the global-visibility flag); `TrendsFilterPreset` (13 fields covering identity, type discriminator, dashboard / metrics-source / panel scope, evaluate-type and source metadata, the JSON `seriesConfig`, and the global-visibility flag); `CompareFilterPreset` (15 fields, same shape as Trends but with the additional `seriesSearchText` / `showPercentiles` / `baselineTestRunId` axes that compare-mode requires). Across all three entities the ownership/org/team columns and `created_at`/`updated_at` timestamps are intentionally excluded — they are emitted via dedicated columns on the audit row. All three entities use camelCase property / snake_case column naming for `organization_id` (`@Column({ name: 'organization_id' }) organizationId!`), so every audit call passes `organizationIdOverride: row.organizationId` (matching the precedent established in PRs 8–11). `GraphPresetsService` and `TrendsPresetsService` are CREATE/DELETE-only — neither service exposes an update endpoint. `ComparePresetsService` carries the full CRUD: CREATE after `repo.save`, UPDATE with explicit pre-update entity load (the existing `findOne` returns a DTO and would lose the constructor prototype that `AuditService.dispatch` consults to resolve `auditableFields`), DELETE before `repo.delete` with the same DTO-loss workaround. The cost is one extra SELECT on the compare-preset update / delete paths; the benefit is faithful before/after diffs without needing to teach `AuditService` about DTO mappings. The three modules each import `AuditModule` and register their resource type — `graph-presets`, `trends-presets`, `compare-presets` — with `AuditResourceRegistry` in `onModuleInit`. Snapshot picks up all three entities (each owns an `organization_id` column) and records the new `auditableFields` arrays. Two new spec files (`graph-presets.service.spec.ts`, `trends-presets.service.spec.ts`) and one new top-level describe block in `compare-presets.service.spec.ts` cover the audit invariants: CREATE/UPDATE/DELETE log shapes with `organizationIdOverride`, before/after diff carry-through on the compare update, and `invocationCallOrder` checks for "log before mutation" on every DELETE. |
| 2026-05-03 | 38 | 3 | 35 | PR13: `benchmark-mutation.service.ts` + `test-runs-config.service.ts` + `test-runs-metrics.service.ts` (bundled — the **results-impacting config** group: every entity in this PR retroactively changes pass/fail or anomaly verdicts on already-completed test runs, which is exactly the audit story compliance asks about). Five new `auditableFields` declarations: `Benchmark` (35 SLO-definition fields: scope keys, source/dashboard linkage, panel identity, metric-SLO triple `evaluate_type`/`requirement_operator`/`requirement_value`, Apdex SLO triple `apdex_threshold_ms`/`min_apdex_score`/`include_failed_requests`, behavior knobs `enabled`/`valid`/`exclude_ramp_up_time`/`average_all`/`match_pattern`/the `validate_with_default_if_no_data*` pair, the embedded `configuration` jsonb that carries the threshold spec, plus alerting `alert_on_breach`/`alert_channels` and `baseline_test_run_id` — `metadata` excluded as a free-form bag); `DsCompareConfig` (8 fields covering the (sut, env, workload, dashboard, panel, metric, metrics_source) tuple plus the `config_data` jsonb — the actual ADAPT thresholds — `config_hash`/`last_modified_at` excluded as derived caches); `ProvisionedTemplateDsCompareConfig` (14 fields for golden-path templates: scope, dashboard linkage, panel/metric identity, the `regex`/`higher_is_better`/`metric_classification` triple, plus `config_overrides`); `ExpectedConfigChange` (6 fields: scope keys + `config_key`/`expected_value`/`description`); `SparseMetricExclusion` (6 fields: scope keys + `dashboard_label`/`metric_name`/`reason`). Ownership / org / team and timestamps excluded across all five (emitted via dedicated audit-row columns). Org-id resolution: `Benchmark`, `ExpectedConfigChange`, and `SparseMetricExclusion` use camelCase property / snake_case column for `organization_id`, so every call site passes `organizationIdOverride: row.organizationId`; `DsCompareConfig` and `ProvisionedTemplateDsCompareConfig` use snake_case `organization_id` directly, so `AuditService.dispatch` reads it off the ref without override. `BenchmarkMutationService` carries the full surface: `create` / `createApdexSlo` (CREATE after `repo.save`); `update` / `updateApdexSlo` (UPDATE with cloned before-snapshot from a direct `repo.findOne` — the existing `queryService.findOne` returns a mapped DTO and would lose the constructor prototype `AuditService.dispatch` consults); `delete` (DELETE before the FK null-out and the actual `repo.delete`); `copyToScope` emits one CREATE per persisted new row and one UPDATE (with cloned-before, refetched-after) per overwrite — per the audit architecture's "one row per entity" rule. `TestRunsConfigService.createExpectedConfigChange` and `.createSparseMetricExclusion` are upserts; the existing find-or-create-or-update flow now logs CREATE on the new branch and UPDATE on the existing branch (with cloned before-snapshot). The two delete methods (`deleteExpectedConfigChange`, `deleteSparseMetricExclusion`) previously deleted by composite key without a pre-fetch; the migration adds a `repo.findOne` so the audit row captures the pre-delete state, gated by `if (existing)` to skip the audit when no row matches. `TestRunsMetricsService` covers four paths: `classifyMetric` upsert (CREATE/UPDATE on `ProvisionedTemplateDsCompareConfig`), `createOrUpdateDsCompareConfig` new branch (CREATE on `DsCompareConfig`; the existing branch delegates to `updateDsCompareConfig` which logs UPDATE), `updateDsCompareConfig` (UPDATE with cloned before-snapshot), `deleteDsCompareConfig` (DELETE before `repo.delete`). `applyGoldenPathClassifications` is intentionally **not** audited — it's a worker-driven system action triggered on test-run completion (with `created_by: 'system:golden-path'`), bucket-2 pattern (auditing it would generate noise on every test-run ingestion without compliance value). Module wiring: `BenchmarksModule` adds `AuditModule` import + `OnModuleInit` that registers `'benchmarks' → Benchmark`; `TestRunsModule` (already wired in PR8 for `'test-runs' → TestRun`) gets four additional registrations — `'expected-config-changes'`, `'sparse-metric-exclusions'`, `'ds-compare-configs'`, `'provisioned-template-ds-compare-configs'`. Snapshot picks up all five entities (each owns an `organization_id` column) and records the new `auditableFields` arrays. Spec adds 28 audit assertions across one new spec file (`benchmark-mutation.service.spec.ts`, full audit-only coverage of the 5 mutation paths), one new spec file (`test-runs-config.service.spec.ts`, both create-and-update branches and both deletes for both entities, including a "skip-audit-on-no-match" assertion for the composite-key deletes), and one new top-level describe block in the existing `test-runs-metrics.service.spec.ts` (the four user-facing paths plus an explicit "applyGoldenPathClassifications does NOT audit" assertion to pin the bucket-2 decision). |
| 2026-05-04 | 27 | 0 | 27 | PR18: `benchmarks` user-facing CRUD verification (no-op). `benchmarks.service.ts` is a thin facade; every controller-level mutation (`create` / `update` / `delete` / `copyBenchmarks` / `createApdexSlo` / `updateApdexSlo`) delegates to `BenchmarkMutationService`, which PR13 already audits end-to-end. PR18 adds zero new `auditService.log*` call sites. The benchmarks-controller path also writes to the same workload-Apdex entities as the test-runs handlers covered by v0.2.47.68 — the duplicate-path overlap is intentional per the brainstorm (each entry point gets its own audit row from its own caller context). Allowlist unchanged: `benchmarks.service.ts` is not on the allowlist (the lint rule sees the static `auditService.log*` calls inside `BenchmarkMutationService` and considers the facade-delegating methods clean). New `apps/api/src/modules/benchmarks/__tests__/controller-audit.spec.ts` wires the real `BenchmarksController` + real `BenchmarksService` facade + real `BenchmarkMutationService` (with repos, `AuthorizationService`, `BenchmarkQueryService`, `BenchmarkTagHelper`, and `AuditService` mocked) and asserts the expected `logCreate` / `logUpdate` / `logDelete` call shape — including `organizationIdOverride: ORG_ID`, before/after diff carry-through on the two UPDATE paths, `copyToScope`'s "one CREATE per persisted row" semantic, and `invocationCallOrder` ordering on the DELETE path (`logDelete` before `repo.delete`). Locks in the chain: any future refactor that bypasses `BenchmarkMutationService` from the controller would fail this spec. |
| 2026-05-04 | 27 | 0 | 27 | PR19: `expected-config-change` repository-layer verification (no-op, scope adjusted). Plan-text scope was "audit the parallel repository path that PR13 didn't cover at the service layer," but the call-graph reality is that `ExpectedConfigChangeRepository` has zero production callers — the class is exported from `apps/api/src/repositories/index.ts` and never injected anywhere. Every real `ExpectedConfigChange` mutation flows through `TestRunsComparisonController` → `TestRunsService` (facade) → `TestRunsConfigService.{create,delete}ExpectedConfigChange`, which PR13 audits end-to-end. Wiring `auditService.log*` calls into `createMany` / `deleteBySystemId` / `deleteByContext` would protect a path with no callers — violates "don't design for hypothetical future requirements." Allowlist therefore unchanged in PR19: removing `apps/api/src/repositories/expected-config-change.repository.ts` from `.audit-migration-allowlist.json` would force the lint rule to trip on the three repository-class mutations and force per-method `// audit-skip:` workarounds, which would pre-empt PR20's `POLICY_EXEMPT` design (PR20 promotes all 7 remaining repository-layer files into a new lint-rule exemption set in a single coherent batch). New `apps/api/src/modules/test-runs/__tests__/expected-config-change-controller-audit.spec.ts` wires the real `TestRunsComparisonController` + real `TestRunsService` facade + real `TestRunsConfigService` (with the four repos, `DataSource`, `AuthorizationService`, `AuditService` mocked, and the six unused sibling sub-services stubbed) and asserts the expected `logCreate` / `logUpdate` / `logDelete` call shape on the user-facing controller path: CREATE on the new-row branch, UPDATE on the existing-row upsert branch (with before/after diff carry-through and cloned `before` snapshot intact), DELETE before `repo.delete` (`invocationCallOrder` check), and "no audit emitted on no-match DELETE" mirrors the PR13 service-spec invariant at the controller boundary. Locks in the chain: any future refactor that bypasses `TestRunsConfigService` from the controller (e.g. a switch to the `ExpectedConfigChangeRepository`'s `createMany` / `deleteBy*` methods) would fail this spec. |
| 2026-05-04 | 32 | 5 | 27 | PR17: `report-template.service.ts` + `report-generation.service.ts` + `report-share.service.ts` + `report-template.controller.ts` + `report-generation.controller.ts` (the controllers were grandfathered onto the allowlist; they have no direct repo mutations and clear once the services they delegate to are audited). Two new `auditableFields` declarations: `ReportTemplate = ['name', 'description', 'system_id', 'test_environment', 'workload', 'sections', 'styling', 'is_default', 'is_adhoc', 'teamId']` (full diff is meaningful per the brainstorm — adhoc templates are still recorded; their `is_adhoc: true` value makes those rows filterable in the audit viewer); `GeneratedReport = ['name', 'test_run_id', 'template_id', 'status']` (deliberately narrow — `share_id` / `html_content` / `pdf_data` excluded as content/secret-shaped, plus everything except identity/scope is bucket-2 anyway under the DELETE-only policy). `ReportTemplate` uses TypeORM camelCase property `teamId` mapped to the `team_id` column, so the field list mixes snake_case (entity-level snake_case props like `system_id` / `is_default`) with camelCase (`teamId`); the snapshot picks it up because `ReportTemplate.organizationId` is mapped to `organization_id`. `GeneratedReport` is **not** in the snapshot (no `organization_id` column on the entity itself — its org is denormalized via `organizationIdOverride` from the parent test_run/template at log time). Service surface: `ReportTemplateService` carries the full CRUD — `create` (`logCreate` after `repo.save`), `update` (cloned pre-mutation `before` via `Object.assign(new ReportTemplate(), template)` for prototype preservation, `repo.update` SQL UPDATE, re-fetch `after` via `findById`, `logUpdate(before, after)`), `delete` (refactored from raw `repo.delete(id)` + affected-row check to load-via-`findById` then `logDelete(template)` then `repo.remove(template)` so the audit row captures the pre-delete snapshot; `findById` already throws `ResourceNotFoundException` on miss, so the affected-row check is no longer needed). The bulk-update helper `clearDefaultInScope` (a query-builder `UPDATE … WHERE … is_default = true` that runs as a side-effect of assigning a new default template) is marked `// audit-skip:` — the new default's UPDATE row already records the `is_default` flip, and the cleared rows are bucket-2 cascade noise. `ReportGenerationService` is DELETE-only on `GeneratedReport`: `delete` calls `logDelete` with `organizationIdOverride: report.template?.organizationId ?? report.test_run?.organizationId` (both relations are loaded eagerly by `findById`, and the dispatch reads it as the override since `GeneratedReport` itself has no `organization_id` column). The full service contains six other repo-mutating methods (`createFromTemplate`, `updateStatus`, `updateFileSize`, `storeHtmlContent`, `updateJobId`, `incrementRetryCount`) — each carries an inline `// audit-skip:` rationale tying the skip to either the DELETE-only policy or the bucket-2 background-pipeline pattern (status flow, file storage, queue retry counters, job tracking). `incrementDownloadCount` uses `repo.increment` which is not in the lint rule's `MUTATION_METHODS` set (save/delete/remove/update/insert), so no `audit-skip` needed. `createAdHocReport` is the wrinkle — it conditionally creates a `ReportTemplate` (real or adhoc) **and** always creates a `GeneratedReport`. The template create is audited (`auditService.logCreate(savedTemplate)`) per the full-CRUD policy on templates; the report create is intentionally not audited per DELETE-only. Lint sees the `auditService.logCreate` call statically (inside the `if (!templateId)` branch) and considers the method audited; an inline comment near `reportRepo.save(report)` documents the deliberate skip for reviewers. `ReportShareService` (which mutates `GeneratedReport` for share-link metadata) carries `audit-skip` on `updateShareSettings`, `regenerateShareId`, and `incrementViewCount`, all under the same DELETE-only policy umbrella — share-toggle flips and view counters are bucket-2 visibility/usage telemetry, and the user-facing audit anchor is the eventual `delete()`. `enableSharing` / `disableSharing` delegate to `updateShareSettings` and have no direct repo calls of their own, so they need no marker. Module wiring: `ReportsModule` adds `AuditModule` import + `OnModuleInit` that registers both `'report-templates' → ReportTemplate` and `'generated-reports' → GeneratedReport` with `AuditResourceRegistry` (both are registered because the per-resource history endpoint can answer queries against either type — templates produce CRUD rows, reports produce DELETE rows). Spec adds 9 new audit assertions: 3 in `report-template.service.spec.ts` (CREATE shape, UPDATE before/after carry-through with cloned `before`, DELETE-before-remove ordering), and 6 in `report-generation.service.spec.ts` (DELETE with `organizationIdOverride` resolved from `test_run`, DELETE preferring the `template`-resolved override when both relations are loaded, DELETE-before-remove ordering, plus 3 negative assertions: `createFromTemplate` does not log, `createAdHocReport` with existing `templateId` does not log, and `updateStatus` / `incrementRetryCount` do not log). The existing two `delete` tests in `report-template.service.spec.ts` were updated for the new findById + remove flow. |
| 2026-05-04 | 33 | 1 | 32 | PR16: `deep-links.service.ts`. Scope realignment vs. the 2026-05-04 brainstorm: the row claimed "URL patterns + generic deep-links full CRUD," but reality is that (a) `UrlPattern` is mutated only by `apps/api/src/modules/test-runs/services/jtl-import.service.ts` via raw-SQL `INSERT ... ON CONFLICT` during JTL ingestion — bucket-2 system-derived path, no `organization_id` column, not an `OwnedResource` — and is therefore reassigned to PR20's `POLICY_EXEMPT` set; (b) `GenericDeepLink` has only one mutation method (`createGeneric`) at both the service and repo layers, so the audit surface is single-call CREATE only, not full CRUD. One new `auditableFields` declaration: `GenericDeepLink = ['profile', 'name', 'url']` (the profile-key, the display name, and the URL template — `organizationId`/`teamId`/`createdBy`/`updatedBy`/timestamps emitted via dedicated audit-row columns). `DeepLink` and `UrlPattern` deliberately remain absent / `null` in the snapshot — DeepLink because per-test-run writes are not audited (high-churn ingestion noise per the brainstorm), URLPattern because the snapshot's owned-entity scan (entities with an `organization_id` column) does not include it. Service emits one `auditService.logCreate` call in `createGeneric` (after the repo round-trip). The four per-test-run methods (`update`, `delete`, `copyToScope`, plus `create` for parity) each carry an inline `// audit-skip: per-test-run DeepLink ...` comment marking the deliberate non-audit decision; locked in by four negative-assertion specs. Repo-layer change: `DeepLinksRepository.createGeneric` was returning `mapToGenericDeepLink(saved)` — a plain object literal that strips the entity prototype — so audit dispatch's `ref.constructor.auditableFields` lookup resolved to `undefined` and the diff was lost. Drop the lossy mapping on the write path and return the TypeORM-saved entity instance; read paths still use `mapToGenericDeepLink` (the narrower API shape there is intentional and unrelated to audit). Lint rule extension: `audit-mutation-must-log` now honors a leading `// audit-skip: <rationale>` comment on the method declaration as a deliberate per-method opt-out — the rationale is mandatory (`/audit-skip:\s*\S/`) so the bare token is still rejected. Spec covers both block- and line-comment forms plus the no-rationale invalid case. Module wires `AuditModule` and registers only `'generic-deep-links' → GenericDeepLink` with `AuditResourceRegistry` — `DeepLink`/`UrlPattern` are not registered (no audit rows are written for them, so a per-resource history endpoint pointed at them would always return empty). Spec adds 1 new audit assertion in the existing `createGeneric` describe (CREATE call shape, with the entity instance verified via `Object.assign(new GenericDeepLinkEntity(), …)`) plus a 4-test `Audit logging — Phase 5a PR16` block of negative assertions for the per-test-run paths. The `deep-links.repository.ts` allowlist entry remains — repository-layer audits are scoped to PR20. |
| 2026-05-04 | 34 | 1 | 33 | PR15: `systems-under-test.service.ts`. Three new `auditableFields` declarations: `SystemUnderTest = ['name']` (the rename detector — destructive-only audit scope per the brainstorm: routine description / tracing / pyroscope / team_id edits produce an empty diff over `['name']` and `AuditService.dispatch` returns no row), `SystemUnderTestTestEnvironment = ['name']` and `SystemUnderTestWorkload = ['name']` (declared for parity, but no service-layer log call sites yet — the cascade deletes from SUT-DELETE are bucket-2 / implied; ingestion-side env/workload writes live in `test-run-lookup.service.ts` (bucket-2, POLICY_EXEMPT in PR20)). Service emits five audit calls: `createSut` logs CREATE on the SUT after the transaction commits (pre-seeded environments + workloads inside the same transaction are not individually audited — bucket-2 cascade noise, implied by the SUT-CREATE row); legacy `create` logs CREATE (vestigial path — no controller currently calls it, but kept lint-clean for future callers); `update` clones the pre-mutation entity via `Object.assign(new SystemUnderTestEntity(), system)` to preserve the prototype, calls `logUpdate` unconditionally — the auditableFields gate at dispatch handles the "rename detection only" semantic (description / tracing_service / team_id changes silently drop); `updatePyroscopeConfig` follows the same shape but never materializes a row in normal flow (pyroscope edits don't touch `name`); `remove` (vestigial) logs DELETE before `repo.remove`. The **live SUT delete path** is `DeleteSystemUnderTestHandler.execute` (raw-SQL cascade); audit is wired there too — handler now loads the SUT row first (`SELECT id, name, organization_id, team_id`), calls `logDelete` before the transaction starts, and the cascaded child-table deletes (test_runs, application_dashboards, environments, workloads, …) are intentionally not individually audited (bucket-2: cascade noise at this volume, implied by the SUT-DELETE row, and the raw `manager.query('DELETE …')` calls don't surface to the audit lint matcher anyway). `SystemUnderTest`'s `organization_id` column uses snake_case property naming (`@Column({ type: 'uuid' }) organization_id!`), matching `AuditService.dispatch`'s `ref.organization_id` lookup — no `organizationIdOverride` needed (mirrors the Organization / Team precedent from PR6/PR7). Module registers only `'systems-under-test' → SystemUnderTest` with `AuditResourceRegistry`; Environment / Workload are deliberately not registered (no audit rows are written for them, so a per-resource history endpoint pointed at them would always return empty). Snapshot picks up `SystemUnderTest = ['name']` (it owns `organization_id`); `SystemUnderTestTestEnvironment` and `SystemUnderTestWorkload` are not in the snapshot (no `organization_id` column). New `delete-system-under-test.handler.spec.ts` with two assertions (404 path, log-before-transaction ordering) plus 5 new `audit logging` assertions in `systems-under-test.service.spec.ts` (CREATE on createSut, CREATE on legacy create, rename UPDATE, description-only UPDATE call shape, DELETE-before-remove on the vestigial path). |
| 2026-05-04 | 35 | 1 | 34 | PR14: `profiles.service.ts`. Three new `auditableFields` declarations: `Profile` (`name`, `description`, `tags`, `readOnly` — top-level identity + visibility/protection flag); `ProfileGrafanaDashboard` (`profile`, `dashboardName`, `dashboardUid`, `grafanaLabel`, `createSeparateDashboardForVariable`, `setHardcodedValueForVariables`, `matchRegexForVariables`, `readOnly` — covers the dashboard reference + variable assignments); `ProfileBenchmark` (21 fields covering the (`profile_id`, `profile_dashboard_id`, `workload_pattern`) scope tuple + `source` + panel identity + the (`evaluate_type`, `requirement_operator`, `requirement_value`) SLO triple + behavior knobs + `tags` + `read_only` — `metadata` excluded as a free-form bag, mirrors PR13's Benchmark precedent). All three entities use camelCase property `organizationId` (with `@Column({ name: 'organization_id' })`) so every audit call passes `organizationIdOverride: row.organizationId`. `ProfileBenchmark`'s `auditableFields` strings are snake_case to match the entity's snake_case property names (`profile_id`, `dashboard_uid`, etc.); only the ownership columns on this entity use TypeORM-style camelCase property names. Service emits `auditService.log{Create,Update,Delete}` for all 9 mutation methods: profile CRUD (`createProfile`/`updateProfile`/`deleteProfile`), dashboard CUD (`createDashboard`/`updateDashboard`/`deleteDashboard`), benchmark CUD (`createBenchmark`/`updateBenchmark`/`deleteBenchmark`). UPDATE handlers clone the pre-mutation entity via `Object.assign(new EntityClass(), entity)` so the audit dispatcher's `ref.constructor.auditableFields` lookup resolves correctly on the diff. `updateDashboard` is the wrinkle: the existing flow uses `repo.update(id, updateData)` (not `repo.save`) and then re-fetches the post-update row via `repo.findOne` — the audit `before` is captured before the in-place mutations begin, the `after` is the re-fetched row. Profile's cascade-delete child rows (`ProfileGrafanaDashboard`, `ProfileBenchmark`) intentionally not individually audited — they are implied by the Profile-DELETE row, mirroring the org-cascade pattern from PR6 and the test-runs cascade pattern from PR8. Module registers three resource types — `profiles`, `profile-dashboards`, `profile-benchmarks` — with `AuditResourceRegistry`. Snapshot picks up all three entities (each owns an `organization_id` column) and records the new `auditableFields` arrays. Spec adds 9 new audit assertions in a top-level `describe('audit logging')` block (3 per entity), with `invocationCallOrder` checks on every DELETE to pin the "log before mutation" ordering. The new profile-level CUD assertions are also the first tests for `createProfile` / `updateProfile` / `deleteProfile` (those service methods previously had no spec coverage). |

## Migration order

Priority is driven by audit value (sensitive credentials and membership changes first), demonstrability (test-runs covers batch + soft-delete), then everything else.

1. **api-keys** — sensitive credentials; marquee use case for `auditableFields`-as-redaction. ✅ PR5.
2. **organizations + members** — membership changes drive most compliance questions. ✅ PR6.
3. **teams + members** — membership changes (parallel to organizations). ✅ PR7.
4. **test-runs (handlers, bucket 1)** — high-volume mutation; demonstrates batched patterns + soft-delete. ✅ PR8.
5. **dynatrace** — sensitive credentials. ✅ PR9.
6. **grafana (instances + dashboards + app-dashboards)** — sensitive credentials. ✅ PR10.
7. **pyroscope-instances + tracing-instances + tracing-services** — sensitive integrations. ✅ PR11.
8. **graph-presets + trends-presets + compare-presets** — user-owned customization presets. ✅ PR12.
9. **results-impacting config** (`benchmark-mutation` + `test-runs-config` + `test-runs-metrics` covering `Benchmark`, `DsCompareConfig`, `ProvisionedTemplateDsCompareConfig`, `ExpectedConfigChange`, `SparseMetricExclusion`) — overrides that retroactively change verdicts on completed runs. ✅ PR13.
10. **profiles** — full CRUD on Profile + ProfileGrafanaDashboard + ProfileBenchmark. ✅ PR14.
11. **systems-under-test** — destructive only (delete + rename). ✅ PR15.
12. **deep-links (org-scoped only)** — `GenericDeepLink` CREATE; per-test-run `DeepLink` writes intentionally skipped (audit-skip markers); `UrlPattern` reassigned to PR20 (bucket-2 ingestion path, not an `OwnedResource`). ✅ PR16.
13. **reports** — templates full CRUD; generated reports DELETE-only. ✅ PR17.
14. **benchmarks user-facing CRUD verification** — no-op (controller delegates to PR13's audited `BenchmarkMutationService`). ✅ PR18.
15. **expected-config-change repository-layer** — direct repo mutations parallel to the service path PR13 already covered. ✅ PR19 (scope adjusted to no-op verification: `ExpectedConfigChangeRepository` has zero production callers; controller-level integration spec locks in the chain through PR13's `TestRunsConfigService`. Allowlist closure deferred to PR20's `POLICY_EXEMPT` repository-layer batch).
16. **Policy exemptions** — promote NO-decisions + bucket-2 system writes + repo-layer follow-ups into a `POLICY_EXEMPT` lint exemption with per-file rationale; closes the allowlist. ⏭ PR20.
17. **PR Final** — cleanup, CLAUDE.md table flip, drift agent disable.

Each migration PR follows the architecture locked in the plan (PR 5+ section): add `static auditableFields = [...] as const` to the entity, paired `auditService.log*` calls in every mutation method, register the resource type → entity class mapping in `AuditResourceRegistry`, remove the file from the allowlist, update this burndown. Per-resource scope decisions for PRs 14–20 are recorded in § "2026-05-04 per-resource brainstorm" below.

## 2026-05-04 per-resource brainstorm

After PR13 closed the results-impacting config bucket, the remaining 35 allowlist entries were enumerated as user-facing resources and walked one by one. Decisions captured as locked input to PRs 14–20.

| Resource | Decision | PR | Rationale |
|---|---|---|---|
| systems-under-test | **Destructive only** — delete + rename (UPDATE only when `name` changes) | 15 | High-signal events (delete propagates to every child resource; rename invalidates dashboards / grafana-sync state). Routine description / metadata edits would generate noise without compliance value. |
| profiles | **Full CRUD** — Profile + ProfileGrafanaDashboard + ProfileBenchmark joins | 14 | Profiles drive what gets evaluated on every test run; join-table mutations directly change ADAPT scope. Treat the joins as part of the profile, not as cascade noise. |
| notification channels | **Skip** | 20 | Already excluded from `auditableFields`-style credential leak risk by virtue of being skipped entirely; per-channel mutations are admin-config events with low compliance demand. |
| deep-links / generic-deep-links / url-patterns | **Org-scoped only** — `URLPattern` + `GenericDeepLink` full CRUD; `DeepLink` (per-test-run) skipped | 16 | URL patterns + generic deep-links are org-wide config (mid-signal). Per-test-run deep links are written on every ingestion — high churn, low value. |
| alert tag filters | **Skip** | 20 | Filters affect which alerts fire but not whether the underlying alert configuration changed; org-wide notification-channel skip applies similarly. |
| reports — templates | **Full CRUD** | 17 | Template definitions are user-curated; full diff is meaningful. |
| reports — generated | **DELETE-only** | 17 | Creates are background-job output (bucket-2 noise); deletes are user actions and worth recording. |
| metrics-sources | **Skip** | 20 | Datasource credentials already redacted by `auditableFields` design; the user-facing CRUD is admin-config with low compliance demand. (Reconsider if a security review surfaces a need.) |
| expected-config-change | **Full CRUD** | 13 (service) + 19 (verification only) + 20 (allowlist) | Already audited at the service layer via PR13's `TestRunsConfigService`. PR19 adds a controller-level integration spec proving the chain (the `ExpectedConfigChangeRepository` class is exported but has zero production callers, so wiring `log*` calls into its `createMany` / `deleteBy*` methods would protect a path with no callers); the repository file moves to PR20's `POLICY_EXEMPT` batch alongside the other 6 repository-layer files. |
| sparse-metric-exclusion | **Full CRUD** | 13 | Already audited end-to-end via PR13's `TestRunsConfigService` + `TestRunsMetricsService`. No follow-up PR needed. |
| benchmarks (user-facing CRUD via controller) | **Full CRUD** | 18 (verification only) | `benchmarks.service.ts` is a thin facade delegating to `BenchmarkMutationService`, audited in PR13. PR18 adds a controller-level integration spec to lock in the chain. |
| benchmarks Apdex SLO endpoints (`POST/PUT /benchmarks/apdex`) | **Full CRUD** (with deliberate overlap to workload-Apdex audit) | 18 | Two entry points (`benchmarks.controller` + `test-runs` handlers) write to the same workload-Apdex entities. v0.2.47.68 covered the test-runs path; PR18 verifies the benchmarks-controller path also flows through `BenchmarkMutationService.{create,update}ApdexSlo` (which PR13 audits). The duplicate-path overlap is intentional: each entry point gets its own audit row from its own caller context. |
| provisioning | **Skip** (inherits from SUT/etc) | 20 | Provisioning composes SUT + environment + workload creates — those are individually audited (or skipped per the SUT decision above). A separate provisioning-level event would either duplicate or contradict the per-resource events. |
| AWR (4 files) | **Skip** | 20 | Out of scope as confirmed by the brainstorm; AWR is analytics output, not configuration. |
| adapt, events, system-derived test-runs services (×5: anomaly, changepoint, dashboard-query, stale-detection, lookup) | **Skip** | 20 | Bucket-2 system-derived writes per the architectural pattern established in PR8. |
| Repository-layer files (api-key, application-dashboard, compare-filter-preset, dynatrace, test-run-configuration, test-run, tracing-service, trends-filter-preset) | **Skip via repo-layer follow-up** | 20 | Repository-layer audit migration is a separate workstream per the api-keys / dynatrace / tracing precedents; promoting to `POLICY_EXEMPT` with a "repo-layer audit deferred to a future phase" rationale closes them in 5a. |

## Notes from PR 4 (this PR)

- The plan's discovery scan regex (`repo(sitory)?\.(save|delete|remove|update)\b`) missed query-builder-style mutations (`createQueryBuilder().delete()`, `createQueryBuilder().insert()`). Six additional files were added to the seed allowlist after the initial lint run flagged them: `test-runs-anomaly.service.ts`, `test-runs-dashboard-query.service.ts`, `application-dashboard.repository.ts`, `compare-filter-preset.repository.ts`, `test-run.repository.ts`, `trends-filter-preset.repository.ts`.
- One pre-existing regression from PR3 (`v0.2.47.51 audit read endpoints`) was uncovered by the lint run: `apps/api/src/modules/audit/audit-query.controller.ts:47` calls `this.authz.isGlobalAdmin(ctx.roles)` directly. The `PR Quality Gate - Test Suite` workflow stopped firing in March, so the regression went unnoticed at merge time. Added that one path to `apps/api/.rbac-migration-allowlist.json` (which Phase 3c had successfully emptied) to keep this PR scope-clean. Migrating it to `getCapabilities()` / `@RequiresCapability` is Phase 3c follow-up work, not Phase 5a.

## Notes from PR 8 (test-runs, bucket 1)

The 12 test-runs files originally on the allowlist split into three coherent audit decisions. PR8 takes bucket 1 only.

**Bucket 1 — user-facing CUD on TestRun (this PR).** Three lint-flagged handlers (`create-test-run`, `update-test-run`, `update-adapt-config`) plus four raw-SQL update handlers and the cascade-delete handler. `TestRun.auditableFields` pinned by the snapshot. Allowlist 51 → 48.

**Bucket 2 — system-derived analytics writes (deferred, stays on allowlist).** These services produce or maintain derived data on a system-driven cadence (BullMQ workers, schedulers, on-ingest computation). Auditing them would generate compliance-irrelevant noise at ingestion-rate volumes:
- `test-runs-anomaly.service.ts` — bulk anomaly result delete during re-evaluation.
- `test-runs-changepoint.service.ts` — changepoint compute / re-compute (saves `DsChangePoints`, deletes during re-evaluation).
- `test-runs-dashboard-query.service.ts` — `createQueryBuilder().insert()` for dashboard query result cache.
- `test-runs-stale-detection.service.ts` — flips `isStale` / `staleDetectedAt` when a run hasn't received data within the SUT's threshold; runs on a scheduler.
- `test-run-lookup.service.ts:270` — `teamRepo.save(newTeam)` auto-creates a Team during test-run ingestion. Notable: this bypasses `TeamsService.create` and therefore the Team CREATE audit row added in PR7. Two reasons it stays unaudited: (1) it's a system action triggered by an API key during test ingestion, not a user-driven team creation; (2) auditing it without the Team CREATE row from PR7's `TeamsService.create` would produce duplicate audit rows for user-driven team creates that go through the lookup service. Decision-document only — no audit code added. Resolution path is to delete the auto-create entirely and require explicit team provisioning, which is out of scope for Phase 5a.

**Bucket 3 — sub-resource CRUD (deferred, separate audit pass per resource).** Each of these owns its own entity and deserves its own `auditableFields` declaration + handler wiring, not a TestRun-shaped audit:
- `test-runs-config.service.ts` — `ExpectedConfigChange` and `SparseMetricExclusion` CRUD.
- `test-runs-metrics.service.ts` — `DsCompareConfig` (and `ProvisionedTemplateDsCompareConfig`) CRUD.
- `test-run-configuration.repository.ts` — `TestRunConfiguration` CRUD.
- `test-run.repository.ts` — soft-delete + status updates (the `deletionStatus` lifecycle column is a worker-driven mark-for-deletion flag, not a user-visible soft delete).

These four files stay on the allowlist. They are tracked under the existing "Remaining services" bullet in the migration order, not under "test-runs."

The `TestRun` entity has a property-name vs. column-name mismatch: `@Column({ name: 'organization_id' }) organizationId?: string`. `AuditService.dispatch` reads `ref.organization_id`, which is `undefined` on TestRun rows — every TestRun audit call site must pass `organizationIdOverride: testRun.organizationId`. This was the only entity in the migration so far where camelCase property naming bit the dispatch — Organization / Team / TeamMember / OrganizationMember / ApiKey all use snake_case property names that match the column names, so their `ref.organization_id` access works. If the dispatch ever gains a fallback that consults TypeORM column metadata to bridge the gap, the override could be dropped from TestRun — but the explicit override is harmless and self-documenting.
